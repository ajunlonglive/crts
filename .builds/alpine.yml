image: alpine/edge
sources:
  - https://git.sr.ht/~lattis/crts
packages:
  - meson
  - ninja
  - ncurses-dev
tasks:
  - setup: |
      cd crts
      meson build
  - build: |
      cd crts
      ninja -C build
  - test: |
      cd crts
      ninja -C build test
