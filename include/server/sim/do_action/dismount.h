#ifndef SERVER_SIM_DO_ACTION_DISMOUNT_H
#define SERVER_SIM_DO_ACTION_DISMOUNT_H
#include "server/sim/action.h"

enum result do_action_dismount(struct simulation *sim, struct ent *e, struct sim_action *sa);
#endif
