#ifndef SERVER_SIM_DO_ACTION_MOUNT_H
#define SERVER_SIM_DO_ACTION_MOUNT_H
#include "server/sim/action.h"

enum result do_action_mount(struct simulation *sim, struct ent *e, struct sim_action *sa);
#endif
