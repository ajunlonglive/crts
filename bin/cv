#!/bin/sh

. bin/.env

valgrind_log="logs/client-valgrind.log"

valgrind --track-origins=yes --leak-check=full -- \
	build/client/crts -a "$asset_path" -l "$client_log" -v3 "$@" 2>"$valgrind_log"

nvim -O "$client_log" "$valgrind_log"

