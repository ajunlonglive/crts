#!/bin/sh -eux

tmp="/tmp/glad_1"

glad2 --api='gl:core=3.3' \
	--extensions='GL_KHR_debug,GL_ARB_base_instance' \
	--out-path="$tmp" c

cp -rvf "$tmp/include" .
cp -rvf "$tmp/src" lib/glad

rm -rf "$tmp"
